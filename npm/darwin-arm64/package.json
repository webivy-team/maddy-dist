{
  "name": "maddy-dist-darwin-arm64",
  "license": "MIT",
  "scripts": {
    "pre-release": "version=$(cat ../maddy-dist/package.json | jq -r '.version') && maddyVersion=$(echo $version | sed -e 's/-[0-9]*$//g' | sed -e 's/^v//g') && jq '.version = \"'$version'\"' package.json > package.json.tmp && mv package.json.tmp package.json"
  },
  "os": [
    "darwin"
  ],
  "cpu": [
    "arm64"
  ],
  "bin": "maddy",
  "version": "v1.11.1-1"
}
